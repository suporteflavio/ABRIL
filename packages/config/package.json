{
  "name": "@abril/config",
  "version": "1.0.0",
  "description": "Shared configuration for ABRIL",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "echo \"No lint configured\" && exit 0",
    "test": "echo \"No tests configured\" && exit 0",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
