{
  "name": "@abril/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared utilities and types for ABRIL",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@abril/config": "*",
    "typescript": "^5.0.0"
  }
}
